from setuptools import setup, find_namespace_packages

setup(name='swapmols',
      version='1.0',
      description='Tool to modify structure files',
      author='Fabian Keller',
      author_email='fabiankeller@wwu.de',
      packages=find_namespace_packages(),
      zip_safe=True)
